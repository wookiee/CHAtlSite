require 'spec_helper'

describe SessionsController do
  describe "POST create" do
    describe "with existing user" do
      pending "should assign user"
      pending "should assign session[:user_id]"
      pending "should redirect to root_url"
    end

    describe "with new user" do
      pending "should create new user"
      pending "should assign session[:user_id]"
      pending "should redirect to edit user"
    end
  end
end
